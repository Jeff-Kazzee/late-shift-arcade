---
project: late-shift-arcade
status: active
module_area: existing directory adopted from the brownfield tree
related_modules: [agent-workspace]
---
# docs

## Purpose

Router for the existing `docs/` directory, discovered during `create-vivary
adopt`. Vivary did not move, rename, or edit anything inside `docs/` — this
index only points at it.

## Read Next

- Existing directory: `docs/` (not managed by Vivary; read it directly)

Keep this as a thin pointer. If `docs/` grows its own `index.md` or
`README.md` later, fold the routing here into that file instead of duplicating it.
